'use client';

import styles from './index.module.scss';

export default function ReleaseNotes() {
  return (
    <main className={styles.main}>
      <div className={styles.description}>
        This is the release notes page for the DEPLOYMENT_SERVICE application.
      </div>
    </main>
  );
}
